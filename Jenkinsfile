pipeline {
    
    stages {
        stage('Build') {
            steps {
                script {
                    // .NET Core build command
                    dotnet build
                }
            }
        }
        
       
    }
}
